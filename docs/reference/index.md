# Package index

## All functions

- [`BuildHist()`](BuildHist.md) : Build a Beta-tree histogram
- [`BuildKDTree()`](BuildKDTree.md) : Build a k-d tree
- [`ConfLevel()`](ConfLevel.md) : Set significance level for regions at
  each tree depth
- [`FindModes()`](FindModes.md) : Identifies distinct modes in
  Beta-trees histogram
- [`PlotHist()`](PlotHist.md) : Plot a 2D Beta tree histogram
- [`SelectNodes()`](SelectNodes.md) : Select nodes in the histogram
- [`SetBounds()`](SetBounds.md) : Add confidence bounds to bounded
  regions in the k-d tree
- [`compute_adjacency_mat()`](compute_adjacency_mat.md) : Compute the
  adjacency matrix of regions in the histogram
- [`is_connected()`](is_connected.md) : Are two regions distinct modes?
